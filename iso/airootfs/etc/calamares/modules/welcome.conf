# BobOS — page d'accueil (100% hors-ligne : pas de geoip, pas de réseau)
---
showSupportUrl:         false
showKnownIssuesUrl:     false
showReleaseNotesUrl:    false

requirements:
    # espace disque minimal (GiB) sur la cible
    requiredStorage:    8
    # RAM minimale (GiB)
    requiredRam:        1.0

    check:
        - storage
        - ram
        - root
        - screen

    required:
        - storage
        - ram
        - root
