# BobOS — montage du support ISO à l'emplacement standard archiso
# (protect Ventoy/USB : le chemin diffère selon le mode de démarrage)
---
dontChroot: true

timeout: 60

verbose: true

script:
    - "/usr/bin/bobos-mount-iso"
