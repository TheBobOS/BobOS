#!/usr/sbin/nft -f
# BobOS — charge le profil de pare-feu actif (« public » par défaut).
# « bob-firewall on maison » bascule firewall-active.nft vers le profil LAN.
include "/etc/bobos/firewall-active.nft"
